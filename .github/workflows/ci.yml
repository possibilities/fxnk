name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Validate repository contracts (static checks first)
        run: tests/validate.sh
      - name: Check pre-push behavior
        run: python3 tests/pre-push.py
