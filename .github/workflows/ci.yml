name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate repository contracts
        run: tests/validate.sh
      - name: Check shell scripts
        run: shellcheck scripts/install.sh tests/validate.sh
